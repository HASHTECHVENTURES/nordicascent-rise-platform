VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-or-publishable-key
# Set to "true" only in local/dev to allow admin self-registration
VITE_ALLOW_ADMIN_SIGNUP=false

# --- Client-phase integrations (not required for MVP) ---
# Offee: manual CSV export + admin score entry in Selection → Step 2.
# Email: set EXTERNAL_INTEGRATIONS.transactionalEmail.connected in
# src/lib/externalIntegrations.ts and deploy send-transactional-email when ready.
