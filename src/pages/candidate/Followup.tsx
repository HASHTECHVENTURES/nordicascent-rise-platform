import StageTasksPanel from "@/components/candidate/StageTasksPanel";

export default function CandidateFollowup() {
  return (
    <StageTasksPanel
      stageId="followup"
      title="Follow-up & Support"
      description="Long-term support — we treat a 6-month check-in as standard from Nordic Ascent"
    />
  );
}
